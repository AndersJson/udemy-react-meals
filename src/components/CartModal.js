import React from "react";

import classes from "./CartModal.module.css";

const CartModal = () => {
	return (
		<div className={classes["cart-modal--overlay"]}>
			<p>The Cart-modal</p>
		</div>
	);
};

export default CartModal;
